import React from 'react'
import AnniversaryPage from './AnniversaryPage'

const page = ({slug}) => {
  const { slug } = params;
  return (
    <AnniversaryPage slug={slug}/>
  )
}

export default page
